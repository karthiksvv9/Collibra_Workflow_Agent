# Create A Collibra Governed Asset Intake Workflow With

Generated by DSC Collibra Workflow Automation Agent.

## Purpose

Create a Collibra governed asset intake workflow with steward approval, conditional reject path, optional relation creation, API failure handling, and complete documentation.

## Pools And Lanes

- Pool `CreateACollibraGovernedAssetWorkflow_pool`: Create A Collibra Governed Asset Intake Workflow With
- Lane: Requester
- Lane: Data Steward
- Lane: Collibra Automation

## BPMN Blocks

- `start` (startEvent) - Start request; lane: Requester
- `validateRequest` (scriptTask) - Validate request; lane: Collibra Automation
- `reviewRequest` (userTask) - Steward review; lane: Data Steward
- `approvalGateway` (exclusiveGateway) - Approved?; lane: Data Steward
- `applyMetadata` (scriptTask) - Apply metadata changes; lane: Collibra Automation
- `rejectedEnd` (endEvent) - Rejected; lane: Requester
- `successEnd` (endEvent) - Completed; lane: Requester

## Sequence Flows

- `flow_start_validate`: `start` -> `validateRequest`; type: normal; rule: normal
- `flow_validate_review`: `validateRequest` -> `reviewRequest`; type: normal; rule: normal
- `flow_review_gateway`: `reviewRequest` -> `approvalGateway`; type: normal; rule: normal
- `flow_gateway_apply`: `approvalGateway` -> `applyMetadata`; type: normal; rule: ${approvalDecision == 'approve'}
- `flow_gateway_reject`: `approvalGateway` -> `rejectedEnd`; type: normal; rule: ${approvalDecision != 'approve'}
- `flow_apply_success`: `applyMetadata` -> `successEnd`; type: normal; rule: normal

## Forms

- `CreateACollibraGovernedAssetWorkflowStartForm`: Workflow Request (7 fields)

## Test Plan

- Validate BPMN structure and missing references.
- Compile every script task and transition listener with Collibra JARs on classpath.
- Simulate approval, rejection, missing required form fields, and API failure paths.
- Upload first to a non-production Collibra tenant and verify package manifest compatibility.

## Retrieved RAG Context

Question: Create a Collibra governed asset intake workflow with steward approval, conditional reject path, optional relation creation, API failure handling, and complete documentation.

Retrieved context:

- score=0.203 source=C:\Users\Mohith\Documents\Codex\2026-05-15\role-you-are-a-senior-principal\docs\rag_training\README.md kind=md
# RAG Training Corpus

Drop organization-specific Collibra workflow material here:

- Exported workflow packages: `.zip`, `.bpmn`, `.app`, `.form`
- Standards and runbooks: `.docx`, `.pdf`
- Asset inventories and relation maps: `.xlsx`
- XML schemas and integration specs: `.xml`
- Java API documentation exports or scraped HTML

The ingestion pipeline extracts text, BPMN structure, form schemas, UUID-like values, Excel header relationships, and source-target relation candidates.